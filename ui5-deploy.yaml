specVersion: "4.0"
metadata:
  name: abap-to-fiori-system
type: application

builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: abap-to-fiori-system
        additionalFiles:
          xs-app.json: xs-app.json
