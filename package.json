{
  "name": "abap-to-fiori-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ui5 serve -o index.html",
    "build": "ui5 build --all",
    "test": "node --test test/*.test.js",
    "build:cf": "ui5 build preload --clean-dest --config ui5-deploy.yaml --include-task=generateCachebusterInfo",
    "deploy:abap:test": "npm run build && fiori deploy --config ui5-deploy-abap.yaml --testMode true",
    "deploy:abap": "npm run build && fiori deploy --config ui5-deploy-abap.yaml",
    "undeploy:abap": "fiori undeploy --config ui5-deploy-abap.yaml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1.32.0",
    "@ui5/cli": "^4.0.53",
    "ui5-middleware-simpleproxy": "3.7.1",
    "ui5-task-zipper": "^3.6.1"
  }
}
